@echo off                                           REM Отключает вывод команд в консоль для чистого вывода

REM GitHub CLI: Создание репозитория с заменой удаленного репозитория

chcp 65001                                          REM Устанавливает кодировку UTF-8 для правильного отображения символов
set "NAME=Method_2025_2026"                         REM Устанавливает имя проекта    
cd /d "F:\+ Метдразработки 2025-2026"               REM Переходит в каталог проекта на диске F:

git init                                            REM Инициализирует новый локальный Git-репозиторий
git branch -M main                                  REM Переименовывает текущую ветку в main (основная ветка)
git remote remove origin >nul 2>&1                  REM Удаляет старый удалённый репозиторий origin (без вывода)
git rm -r --cached . >nul 2>&1                      REM Очищает индекс Git от отслеживания файлов (без вывода)
git add .                                           REM Добавляет в индекс все файлы (новые и изменённые)
git commit -m "Первый коммит"                       REM Создаёт коммит с сообщением "Первый коммит"
gh repo create %NAME% --private --source=. --push   REM Создаёт приватный репозиторий на GitHub, пушит файлы

pause                                               REM Пауза для просмотра результата в консоли
